{% import "/_macros" as macros %}

{% if block.sectionImage|length %}
    {{ macros.responsiveImage(block.sectionImage.one, { pictureClass: 'layer__image' }) }}
{% else %}
    <svg viewBox="0 0 16 9" style="background: #0002"></svg>
{% endif %}
