<div class="layer html-embed {% if not blockLoop.first %}pt-5{% endif %}">
    <div class="container">
        {% if block.sectionTitle %}
            <h2>
                {{ block.sectionTitle }}
            </h2>
        {% endif %}

        {{ block.embed|raw }}
    </div>
</div>
