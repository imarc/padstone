<div class="layer call-to-action {% if not blockLoop.first %}pt-5{% endif %}">
    <div class="container">
        <div class="row justify-content-md-center align-items-center">
            <div class="col-md-6">
                {% if block.sectionTitle %}
                    <h2>
                        {{ block.sectionTitle }}
                    </h2>
                {% endif %}

                {{ block.blockContent }}
            </div>

            <div class="col-md-3">
                <p class="text-center mb-0">
                    {{ block.cta.link({class: 'btn btn-primary btn-lg'}) }}
                </p>
            </div>
        </div>

        <hr class="mt-5">
    </div>
</div>
