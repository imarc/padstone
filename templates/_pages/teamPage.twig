{% set section = 'teamPage' %}
{% extends "_layouts/main" %}
{% block content %}
    {% include '_partials/breadcrumbs' with { showBreadcrumbs: entry.showBreadcrumbs } %}

     {% include '_partials/hero' with { hero: entry.hero.eagerly().one() } %}

    <div class="container">
        <div class="tiles">
            {% for person in craft.entries.section('team').eagerly().all() %}
                <div class="tiles__tile card">
                    <div class="card__container">
                        <div class="card__heading">
                            {{ person.title }},
                            {{ person.jobTitle }}
                        </div>
                        <div class="card__content">
                            {{ person.abstract }}
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
