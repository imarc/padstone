<header class="section__header">
    {% if block.sectionEyebrow %}
        <div class="eyebrow -primary">{{ block.sectionEyebrow }}</div>
    {% endif %}
    {% if block.sectionHeading %}
        <h2 class="section__heading">{{ block.sectionHeading }}</h2>
    {% endif %}
    {% if block.sectionSubheading %}
        <h3 class="section__subheading">{{ block.sectionSubheading }}</h3>
    {% endif %}
</header>
